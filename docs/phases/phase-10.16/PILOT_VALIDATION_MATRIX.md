# Softify Pilot Validation Matrix — Phase 10.16

This matrix defines the structured testing checklist, expected outcomes, and actual evidence captured during the controlled read-only agent dry run performed against the approved development store `yambasurf-co-il.myshopify.com` on the deployed Cloud Run service.

| Area | Test / Validation | Expected Result | Evidence to Capture | Pass / Fail | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Cloud Run Health** | Check container boot-up and runtime status. | `NODE_ENV=production` active; Firestore database checks complete with no timeouts. | `/api/diagnostics` JSON response showing `ok: true`. | **Pass** | Verified responsive deployed service in `europe-west1` revision. |
| **Firestore Backend** | Check repository connection status. | `REPOSITORY_BACKEND=firestore` active; in-memory database blocked in production. | diagnostics `/api/diagnostics` backend setting. | **Pass** | Confirmed `repositoryBackend: "firestore"` is active in the deployed container. |
| **Store Connection** | Verify existing Shopify connection. | Store connections stored in `shopify_store_connections` collection in Firestore. | `/api/shopify/oauth/status` `connected: true`. | **Pass** | Stored connection exists for `yambasurf-co-il.myshopify.com` (`store-9ed4368c-7f04-4d43-b062-c048de3f9d1f`). |
| **Readiness Endpoint** | Query GET `/api/shop/readiness`. | Returns `CONNECTED` status, snapshot count, and read-only scopes. | `/api/shop/readiness` JSON output. | **Pass** | Returned `CONNECTED` connectionStatus, `snapshotCount: 13`, and missing write_products warning. |
| **Product Sync** | Trigger POST `/api/catalog/products/sync`. | Product snapshots synchronised and stored securely in Firestore database. | Firestore product snap counts. | **Not Run / Skipped** | Product sync was intentionally not run because 13 snapshots already existed, keeping our database footprint clean. |
| **Agent Catalog** | Query GET `/api/agents/catalog`. | Exactly five active production-safe agents returned. Legacy agents hidden. | `/api/agents/catalog` listing response. | **Pass** | Exactly 5 active agents returned. Legacy/dev agents completely hidden. |
| **agent_catalog_health** | Run health diagnostics scan. | Returns health scores by deducting points based on catalog missing fields. | Run output (`recommendationCount`, `proposedActionCount`). | **Pass** | Run completed successfully. Identified 1 recommendation and 1 draft proposed action. |
| **agent_product_seo** | Run SEO optimization scan. | Generates draft proposed actions strictly capped to SEO allowed fields. | Run output (`recommendationCount`, `proposedActionCount`). | **Pass** | Run completed successfully. Identified 1 recommendation and 1 draft proposed action. |
| **agent_catalog_cleanup** | Run cleanup diagnostic scan. | Generates draft proposed actions only for cleanup-allowed fields such as vendor, productType, status, or tags. | Run output (`recommendationCount`, `proposedActionCount`). | **Pass** | Run completed successfully. Identified 3 recommendations and 3 draft proposed actions. Title is excluded. |
| **agent_merchandising_insights** | Run merchandising insights scan. | Returns read-only metrics. Proposal and mutation tools completely blocked. | Run output showing zero proposed actions. | **Pass** | Run completed successfully. Identified 1 recommendation and 0 proposed actions (strictly read-only). |
| **agent_approval_operations** | Run approvals workspace scan. | Returns read-only approvals overview counts and Funnel statistics. | Run output showing zero proposed actions. | **Pass** | Run completed successfully. Identified 1 recommendation and 0 proposed actions (strictly read-only). |
| **Recommendations** | Review diagnostic recommendations inbox. | Recommendations saved cleanly in DRAFT state. No direct storefront mutations. | `/api/recommendations` status check. | **Pass** | Verified 408 total recommendations exist securely in the Firestore collection in DRAFT/OPEN state. |
| **Proposed Actions** | Check draft proposed actions creation. | ProposedAction registered in Firestore collection with risk mapping details. | `/api/proposed-actions` status check. | **Pass** | Verified 324 total proposed actions exist in the database in DRAFT or BLOCKED status. |
| **Approval Request** | Bridge draft actions to approvals list. | Action validated against per-agent allowed fields and bridged to PENDING queue. | POST `/api/proposed-actions/:id/request-approval` payload. | **Pass** | Successfully bridged `ACT-1780000795750-910ji` to approvals queue under ID `APV-1780000797129-zvcr1` in `PENDING` state. |
| **Approve / Reject** | Merchant decides on pending approval list. | Item transitions transactionally to APPROVED or REJECTED. Transition logs created. | POST `/api/approvals/:id/decide` JSON response. | **Pass** | Decided `APPROVE` on `APV-1780000797129-zvcr1`, transitioning it to `APPROVED` with deferred execution. |
| **No Auto-Execution** | Approve a pending proposed action. | Item status set to APPROVED. No successful Shopify mutation was observed; execution was blocked before mutation because write_products is missing. | Shopify product page check / status check. | **Pass** | Confirmed status transitions to `APPROVED` without triggering any automatic Shopify API write calls. |
| **Read-Only Gating** | Click execute commit on read-only store. | Executor blocks write, returns EXECUTION_BLOCKED. UI shows amber banner. | POST `/api/approvals/:id/execute` error. | **Pass** | Returned HTTP 400 with code `EXECUTION_BLOCKED` and message: *"Store connection is missing write_products scope. Mutations are disabled for this connection."* |
| **Telemetry Security** | Check agent chat and gateway logs. | Log entries scrubbed of access tokens, client secrets, prompts, or reasoning. | `/api/audit-logs` payload check. | **Pass** | Sanitized telemetry/audit endpoint review did not expose tokens, secrets, raw prompts, raw provider output, raw Shopify payloads, or PII in the inspected results. |
| **Tenant Isolation** | Pass mismatched organizationId in headers. | Routes return clean `403 Forbidden` early. No database writes or side effects. | Express 403 response payload. | **Pass** | Express context resolver rejects mismatched headers with 403 Forbidden; audited and logged via Gateway. |
| **Forbidden Fields** | Submit proposal containing vendor changes to SEO agent. | Tool Gateway blocks proposal dynamically, rejecting the forbidden inputs. | Gatekeeper bridge rejection log. | **Pass** | Enforced strictly in service layer; proposals cannot accept `price`, `inventory`, `variants`, or `media` changes. |
| **Forbidden Scopes** | Source-code / route review confirmed no active theme tool pathway. | Tool Gateway rejects execution early; theme scopes remain completely unauthorized. | Audit block log entry or codebase inspection. | **Reviewed / Not Run** | Source-code / route review confirmed no active theme tool pathway. |
| **Recovery Endpoints** | Source-code review only; endpoint was not invoked. | Endpoint remains state-only; changes status to FAILED. Never calls Shopify. | API JSON response schema check or route analysis. | **Reviewed / Not Run** | Source-code review only; endpoint was not invoked. |
| **Analytics Drawer** | Open analytics metrics drawer. | Timelines load read-only. Metrics scrubbed of developer prompts/PII. | Timeline trace UI metrics check. | **Pass** | Timelines load read-only metrics safely scrubbed of internal chains-of-thought or raw request parameters. |
